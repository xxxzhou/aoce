
#include "base.hpp"
int32_t add(int32_t a, int32_t b) { return a + b; }

void main() {
    float x = pow(4, -1);
    float xx = pow(4, -0.5);
    float xxx = pow(16, 0.25);
    float xxxx = pow(16, -0.25);
}